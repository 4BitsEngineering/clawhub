# clawhub v0 — Especificación

> Objetivo: 2 semanas hasta v0 demoable + funcional para la primera instalación in-situ.
> Versión: 0.1 · Última iteración: 2026-05-16.

---

## 1. Goal y scope v0

**Objetivo medible v0:**
> Podemos instalar `clawgents-desktop` en el PC de un trabajador de la primera asesoría, parearlo a la firma desde clawhub, y verlo vivo en el Admin Console con telemetría actualizada cada minuto.

### In-scope v0

- [ ] Phone-home protocol + endpoint sink.
- [ ] Pairing flow con token de uso único.
- [ ] Auth básica (operator + firm admin) con magic link.
- [ ] Admin Console operator: lista de firmas + detalle.
- [ ] Admin Console firma: lista de instancias + detalle básico.
- [ ] Persistencia: firmas, usuarios, instancias, heartbeats, pairing tokens.

### Out of scope v0 (vienen en v0.5 / v1)

- ⏭ Skills push channel (Tier Firma).
- ⏭ License + billing (Stripe).
- ⏭ Channel broker WebSocket inter-instancia.
- ⏭ Auto-update channel.
- ⏭ Onboarding self-service de firmas (las primeras se crean a mano).
- ⏭ Métricas avanzadas, dashboards, alertas.
- ⏭ Audit log.

---

## 2. Domain model

```
Firm (tenant)
├── id (uuid)
├── name (string)
├── plan (enum: 'starter' | 'pro' | 'enterprise')   // informativo en v0
├── seats_purchased (int)
├── created_at
└── relations
    ├── users (1:N)
    ├── instances (1:N)
    └── pairing_tokens (1:N)

User
├── id (uuid)
├── email (unique)
├── role (enum: 'operator' | 'firm_admin')
├── firm_id (nullable FK — null para operators)
├── created_at
└── magic links via NextAuth (no password)

Instance
├── id (uuid)                                       // generated by clawhub at pairing
├── instance_token (secret, hashed)                 // for heartbeat auth
├── firm_id (FK)
├── worker_label (string)                           // 'María García', 'PC-Recepción', etc.
├── version (string)                                // e.g. '0.3.0'
├── os (string)                                     // 'win10', 'win11'
├── status (enum: 'online' | 'offline' | 'unpaired')
├── last_heartbeat_at (timestamp)
├── created_at
└── relations
    └── heartbeats (1:N)

Heartbeat
├── id (uuid)
├── instance_id (FK)
├── received_at (timestamp)
├── cpu_pct (number)
├── ram_mb (number)
├── tokens_consumed_24h (int)
├── uptime_s (int)
├── last_error (text, nullable)
└── raw_payload (jsonb)                             // forward compat

PairingToken
├── id (uuid)
├── firm_id (FK)
├── code (string, 6-8 chars)                        // human-readable
├── expires_at (timestamp)                          // 10 min default
├── used_at (timestamp, nullable)
└── created_at
```

Convenciones:
- Todos los IDs son UUID v4.
- Timestamps en UTC ISO 8601.
- `instance_token` se almacena hasheado (bcrypt/argon2), nunca en claro.
- `heartbeats` se rotan: mantenemos último N o últimas 24h (decisión pendiente, v0 sin rotación).

---

## 3. Phone-home protocol

### 3.1 Pairing — registrar una instancia nueva

```
POST /api/v0/pair
Content-Type: application/json

{
  "pairing_code": "ABC-123",
  "worker_label": "María García",
  "version": "0.3.0",
  "os": "win11"
}
```

Response 200:
```json
{
  "instance_id": "uuid",
  "instance_token": "long-secret-string",
  "firm_id": "uuid",
  "firm_name": "Asesoría Pérez"
}
```

Response 400 / 410: token inválido o expirado.

Flujo:
1. Socio/admin firma abre clawhub → Firm Console → "Add worker" → genera código 6-8 chars válido 10 min.
2. Worker abre `clawgents-desktop` por primera vez → wizard pide código.
3. Desktop hace POST `/api/v0/pair` con código.
4. Server valida, crea `Instance`, emite `instance_token`, marca `PairingToken` como usado.
5. Desktop guarda `instance_token` en config local, arranca heartbeat loop.

### 3.2 Heartbeat — telemetría continua

```
POST /api/v0/heartbeat
Authorization: Bearer <instance_token>
Content-Type: application/json

{
  "instance_id": "uuid",
  "version": "0.3.0",
  "uptime_s": 14523,
  "cpu_pct": 12.4,
  "ram_mb": 512,
  "tokens_consumed_24h": 47821,
  "last_error": null,
  "extras": { /* anything else, forward-compat */ }
}
```

Response 200:
```json
{
  "ok": true,
  "next_heartbeat_in_s": 60,
  "commands": []
}
```

`commands` queda preparado para v0.5+ (pedirle a la instancia que se reinicie, que tire una update, etc.). En v0 siempre vacío.

**Frecuencia:** cada 60s.
**Auth:** Bearer token específico de la instancia.
**Idempotencia:** heartbeats se acumulan, no se deduplican (cada heartbeat es un punto en el tiempo).

### 3.3 Estados de instancia

| Estado | Cuándo | Cómo se determina |
|---|---|---|
| `unpaired` | Token generado pero aún no usado | `PairingToken` existe, `Instance` no |
| `online` | Heartbeat recibido en últimos 3 min | `last_heartbeat_at > now() - 3min` |
| `offline` | Último heartbeat > 3 min | `last_heartbeat_at <= now() - 3min` |

(Computado en query, no almacenado.)

---

## 4. Auth

NextAuth.js con magic link por email. Sin password.

**Roles:**
- `operator`: tú y tu equipo. Sin `firm_id`. Ven todas las firmas.
- `firm_admin`: socio/responsable de una firma. Con `firm_id`. Solo ve su firma.

**Creación de usuarios v0:**
- `operator`: se crean manualmente vía seed/script (sin self-signup).
- `firm_admin`: lo crea un `operator` al dar de alta una firma.

Workers **no** entran en clawhub. Solo en `clawgents-desktop`.

---

## 5. Admin Console — Screens v0

### 5.1 Pantallas operator

| Ruta | Qué muestra |
|---|---|
| `/login` | Magic link form |
| `/operator` | Dashboard: tabla de firmas (name, n_instances_online/total, plan, MRR, creada) |
| `/operator/firms/[id]` | Detalle firma: datos, tabla de instancias, botón "Add firm_admin user" |
| `/operator/firms/new` | Crear firma (name, plan, seats, primer firm_admin email) |

### 5.2 Pantallas firma (firm_admin)

| Ruta | Qué muestra |
|---|---|
| `/login` | Magic link form |
| `/firm` | Dashboard firma: tabla de instancias (worker, estado, versión, last_seen, RAM/CPU, tokens 24h) |
| `/firm/pair` | "Add worker" — genera pairing code visible 10 min con QR opcional |
| `/firm/instances/[id]` | Detalle instancia: histórico de heartbeats (últimos N), uptime, errores |

Total: **7 pantallas funcionales**. Cero polish.

---

## 6. Stack y decisiones técnicas

| Componente | Elección | Razón |
|---|---|---|
| Framework | Next.js 16 App Router (Turbopack) | Última estable; ojo: rompe con Next 15 en algunas APIs (ver `AGENTS.md`) |
| Lenguaje | TypeScript strict | Seguridad del modelo de dominio |
| DB | PostgreSQL (Supabase) | Robusta, jsonb para extras forward-compat |
| ORM | **Prisma** | Maduro, schema declarativo, migrations |
| Auth | Auth.js v5 (NextAuth) + Resend (magic link) | Cero password, suficiente v0 |
| UI | shadcn/ui + Tailwind v4 | Estética seria sin diseño desde cero |
| Hosting app | Vercel | Deploy 1-click, alineado con Next.js |
| Hosting DB | **Supabase** (decidido) | Free tier 500MB + tooling rico (auth, realtime para v0.5+) |
| Email | Resend (magic link) | Simple, gratis bajo cuotas |
| Tests | Vitest | Ligero |
| Lint/format | ESLint + Prettier | Estándar |

### Open decisions de stack (resueltas)

- [x] **Prisma vs Drizzle**: ✅ Prisma.
- [x] **Postgres host**: ✅ Supabase.
- [x] **Multi-tenant strategy**: ✅ column-based (`firm_id` en cada tabla relevante). V0 suficiente.
- [ ] **Secrets management**: Vercel env vars suficientes v0, considerar Doppler/Infisical después.

---

## 7. Setup local

🚧 **Pendiente scaffold inicial.** Pasos previstos:

```bash
# 1. Scaffold
npx create-next-app@latest clawhub --typescript --tailwind --app --src-dir --import-alias "@/*"

# 2. Dependencias
npm install @prisma/client next-auth @auth/prisma-adapter
npm install -D prisma
npx prisma init

# 3. shadcn/ui
npx shadcn@latest init
npx shadcn@latest add button card table form input label

# 4. Schema + migration
# Editar prisma/schema.prisma con domain model § 2
npx prisma migrate dev --name init

# 5. Seed initial operator user
node prisma/seed.ts

# 6. Run
npm run dev
```

---

## 8. Roadmap v0 — Semana a semana

### Semana 1: Backend + telemetría

**Días 1-2 — Scaffold:**
- [ ] `create-next-app` + Prisma + NextAuth + shadcn.
- [ ] Schema Prisma con domain model.
- [ ] Seed: 1 operator user (tu email).
- [ ] Migrations corriendo en local + en Postgres managed.

**Días 3-4 — Endpoints:**
- [ ] POST `/api/v0/pair` — pairing flow.
- [ ] POST `/api/v0/heartbeat` — telemetría.
- [ ] Middleware: validación Bearer token instancia.
- [ ] Tests básicos de los dos endpoints.

**Día 5 — Pairing UX end-to-end:**
- [ ] Pantalla `/firm/pair` para generar código.
- [ ] Mock cliente que pairea y heartbea (script bash o pequeño Node) — para validar sin esperar a `clawgents-desktop`.

### Semana 2: Admin Console + cierre v0

**Días 6-7 — Console operator:**
- [ ] `/operator` lista firmas.
- [ ] `/operator/firms/[id]` detalle.
- [ ] `/operator/firms/new` crear firma.

**Días 8-9 — Console firma:**
- [ ] `/firm` lista instancias con estado live.
- [ ] `/firm/instances/[id]` detalle + histórico heartbeats.
- [ ] Polling (o SWR refresh) para que `online/offline` se actualice solo.

**Día 10 — Deploy + integración con clawgents-desktop:**
- [ ] Deploy a Vercel + Postgres managed.
- [ ] Implementar phone-home en `clawgents-desktop` (cliente real, no mock).
- [ ] Smoke test E2E: install desktop → pairing → ver instancia online en console real.

---

## 9. Definition of Done v0

clawhub v0 está listo cuando:

- ✅ Un operator puede crear una firma y dar de alta su primer firm_admin desde la UI.
- ✅ El firm_admin recibe magic link, entra, genera un pairing code.
- ✅ `clawgents-desktop` consume ese código vía wizard de primer arranque, se registra, recibe `instance_token`.
- ✅ El desktop heartbea cada 60s. La instancia aparece `online` en la UI de firma y se ve `offline` cuando el desktop se cierra.
- ✅ El operator ve agregado de todas las firmas (cuántas online, cuántas offline, total seats).
- ✅ Sistema deployado en producción real, no localhost.

---

## 10. Lo que viene después de v0

| Versión | Bloque | Qué añade | Estimado |
|---|---|---|---|
| **v0.5** | Skills push | Endpoint para que clawhub publique skills/SOPs y las instancias las descarguen | 1-2 semanas |
| **v0.5** | Billing | Stripe + plans + seat enforcement | 1-2 semanas |
| **v0.6** | Update channel | Release management de versiones de desktop | 1 semana |
| **v1** | Channel broker | WebSocket broker para Tier Equipo | 2-3 semanas |
| **v1** | Audit log | Quién hizo qué, cuándo | 1 semana |
| **v1.5** | Self-service signup | Las firmas se pueden dar de alta solas con tarjeta | 2 semanas |

No comprometerse a fechas más allá de v0 hasta tener las primeras 2 instalaciones reales y aprender qué falta de verdad.

---

## 11. Riesgos v0

| Riesgo | Mitigación |
|---|---|
| El phone-home cliente en `clawgents-desktop` no está construido todavía | Empezar con mock (script Node que simula heartbeats); cliente real en día 10 |
| Magic link en NextAuth requiere proveedor email funcionando | Resend gratis hasta 100/día, suficiente v0 |
| Postgres managed cuesta dinero antes de tener clientes | Supabase / Neon free tier suficiente para v0 + primeros clientes |
| Multi-tenant data leakage (un firm_admin viendo otra firma) | Tests específicos de aislamiento; middleware de auth comprueba `firm_id` en cada query |
| Estado `online/offline` lag por polling | 3 min threshold da margen; v0 acepta lag, no tiempo real |

---

## 12. Open questions

- [ ] Naming: ¿`clawhub` definitivo o queremos repensar? (Familia `claw*` ya tiene clawcrew, clawgents).
- [ ] Repo GitHub: ¿bajo qué organización? ¿`4BitsEngineering/clawhub` (consistente con clawcrew) o personal?
- [ ] License: ¿código privado/cerrado o source-available (BSL/Elastic License)? clawhub es el componente comercial.
- [ ] Branding del producto que verán los clientes: ¿`clawhub` se ve en la UI o aparece otro nombre comercial?
- [ ] Idioma UI: ¿es/en ambos desde día 1 o solo es para v0 (las dos asesorías son ES)?
